* explain more clearly how to access columns from a 'data.frame' object, including the fact that a single value on the bracket operator selects a column.

* include how to add columns into a 'data.frame' object.
